apiVersion: v1
kind: Service 
metadata: 
  name: ci-svc
  labels: 
    app: myapp 
    env: devops
spec: 
  type: ClusterIP
  selector: 
    app: myapp
  ports: 
  - name: mysvcport 
    protocol: TCP
    port: 80
    targetPort: 80