apiVersion: v1
kind: Service 
metadata: 
  name: np-svc
  labels: 
    app: myapp 
    env: devops
spec: 
  type: NodePort 
  selector: 
    app: myapp 
  ports: 
  - name: np-svc-port
    protocol: TCP
    port: 80
    targetPort: 80